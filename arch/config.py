# Global configuration and special tokens.

# Special tokens prepended to the vocabulary.
VOCAB_HEADS = ['<pad>', '<start>', '<end>', '<unk>']

# Sentence separator token: marks the boundary between sentences/utterances.
SEP = '_'
